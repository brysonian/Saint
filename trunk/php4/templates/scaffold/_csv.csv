<?php

	header('Content-Type: text/csv');
	header("Content-Disposition: attachment; filename=$filename.csv");
	
	# header
	unset($items[0]['id']);
	unset($items[0]['uid']);
	echo join("\t", array_keys($items[0]));
	echo "\n";
	


	foreach ($items as $k=>$item) {
		unset($item['id']);
		unset($item['uid']);
		echo join("\t", $item);
		echo "\n";
	}

?>
